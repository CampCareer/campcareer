import Link from "next/link"
import { ES_COMMUNITIES, ES_OCCUPATIONS, ES_UNIVERSITIES, isSpainOccupationIndexable } from "@/data/es-map-data"
import { pageMetadata } from "@/lib/seo"
export const metadata = pageMetadata({ title: "Spain study-to-work decision data | CampCareer", description: "Spain source-led comparison of province shortage signals, CNO wages, rent and RUCT universities for international graduates.", path: "/countries/spain" })
export default function SpainCountry() { return <main className="mx-auto max-w-5xl px-4 py-12"><p className="text-sm font-semibold text-blue-700">Spain</p><h1 className="mt-4 text-4xl font-semibold">Spain study-to-work decision data</h1><p className="mt-4 max-w-3xl leading-7 text-slate-600">SEPE 부족 목록은 도별 외국인 고용 접근성 신호이며, INE 임금과 SERPAVI 주거비는 별도 근거로 비교합니다.</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><Fact value={`${ES_COMMUNITIES.length} communities`} label="Map regions" /><Fact value={`${ES_OCCUPATIONS.filter(isSpainOccupationIndexable).length} occupations`} label="SEPE indexable profiles" /><Fact value={`${ES_UNIVERSITIES.length} universities`} label="RUCT pins" /></div><Link href="/es" className="mt-8 inline-flex text-sm font-semibold text-blue-700">Open Spain Maps and data</Link></main> }
function Fact({ value, label }: { value: string; label: string }) { return <div className="rounded-lg border border-slate-200 p-4"><p className="font-semibold">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></div> }
