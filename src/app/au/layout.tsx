import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Australia | Study & Work | CampCareer",
  description: "Explore job salaries, visa pathways, and university options for working in Australia. Find your ideal occupation with real salary data.",
}

export default function AuLayout({ children }: { children: React.ReactNode }) {
  return children
}
