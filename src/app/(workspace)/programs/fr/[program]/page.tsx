import type { Metadata } from "next"
import Link from "next/link"
import { notFound, permanentRedirect } from "next/navigation"
import { ArrowLeft, ArrowUpRight, Building2, CalendarClock, Clock3, GraduationCap, Languages, MapPin, ShieldCheck } from "lucide-react"
import { getFrProgramBySlug } from "@/lib/programs/fr-programs.server"
import { frProgramDetailPath, isIndexableFrProgramSlug } from "@/lib/programs/fr-program-seo"
import { institutionDetailPath } from "@/lib/institutions/institution-search"

const BASE_URL="https://www.campcareer.com"
const FR_PUBLIC_INSTITUTION_SLUGS=new Set(["aix-marseille-universite","sorbonne-universite","universite-cote-dazur","universite-de-bordeaux","universite-de-strasbourg","universite-grenoble-alpes","universite-paris-cite","universite-paris-saclay","universite-psl"])
type Params={params:Promise<{program:string}>}
function safeSlug(value:string){const slug=value.trim().toLowerCase();return/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(slug)?slug:null}
function safeUrl(value:string|null){if(!value)return null;try{const url=new URL(value);return url.protocol==="https:"||url.protocol==="http:"?url.toString():null}catch{return null}}
function durationLabel(months:number|null){if(!months)return"Not published";if(months%12===0)return`${months/12} ${months===12?"year":"years"}`;return`${months} months`}
function admissionCopy(status:string,verification:string){if(verification==="unverified"||verification==="stale"||verification==="rejected")return{label:"Current international admission not verified",body:"The programme exists, but CampCareer has not verified a current international application route for this programme.",className:"border-[#e5e1d7] bg-[#faf9f5] text-[#6f6d68]"};if(status==="closed")return{label:"Verified 2026 application cycle closed",body:"The programme remains source-verified, but the verified 2026 application period is over. CampCareer does not infer a 2027 window until a current source publishes it.",className:"border-[#eed8cf] bg-[#fff8f5] text-[#9a4e39]"};if(status==="restricted")return{label:"Applicant-specific application route",body:"The applicable route can depend on programme level, nationality or residence and may run through Mon Master, the university, or Études en France. Programme existence does not mean applications are universally open.",className:"border-[#eed8cf] bg-[#fff8f5] text-[#9a4e39]"};if(status==="open")return{label:"Source-backed applications open",body:"A current official source supports an active application route. Recheck the provider before submitting because deadlines and applicant routes can change.",className:"border-[#cfe0c9] bg-[#f4f9f2] text-[#37672c]"};if(status==="not_yet_open")return{label:"Next verified cycle not yet open",body:"A future application cycle is source-backed, but it is not open today.",className:"border-[#d9e3f7] bg-[#f7f9ff] text-[#315f9f]"};return{label:"Programme verified · current window unknown",body:"The programme is source-verified, but CampCareer has not inferred a current or future international application window without stronger current evidence.",className:"border-[#d9e3f7] bg-[#f7f9ff] text-[#315f9f]"}}
async function loadProgram(segment:string){const slug=safeSlug(segment);return slug?getFrProgramBySlug(slug):null}
export async function generateMetadata({params}:Params):Promise<Metadata>{const{program:segment}=await params;const program=await loadProgram(segment);if(!program)return{title:"Program not found",robots:{index:false,follow:false}};const canonical=frProgramDetailPath(program.slug);return{title:`${program.title} · ${program.institutionName}`,description:`${program.title} at ${program.institutionName}. Programme identity and French applicant routes are tracked separately.`,alternates:{canonical:`${BASE_URL}${canonical}`},robots:{index:isIndexableFrProgramSlug(program.slug)&&program.internationalAdmissionStatus!=="closed",follow:true}}}
export default async function FrProgramDetailPage({params}:Params){const{program:segment}=await params;const program=await loadProgram(segment);if(!program)notFound();const canonicalPath=frProgramDetailPath(program.slug);if(segment!==program.slug)permanentRedirect(canonicalPath);const admission=admissionCopy(program.internationalAdmissionStatus,program.admissionVerificationStatus);const institutionPath=program.institutionSlug&&FR_PUBLIC_INSTITUTION_SLUGS.has(program.institutionSlug)?institutionDetailPath("FR",program.institutionSlug):null;const programmeUrl=safeUrl(program.officialProgramUrl);const admissionUrl=safeUrl(program.admissionSourceUrl);return <div className="mx-auto max-w-5xl"><Link href="/programs?country=FR" className="inline-flex items-center gap-1.5 text-[12px] font-semibold text-[#6f6d68] hover:text-[#3e7a2e]"><ArrowLeft className="size-3.5"/> Back to France programs</Link><div className="mt-5 grid gap-6 lg:grid-cols-[minmax(0,1fr)_300px] lg:items-start"><main className="min-w-0"><header className="rounded-2xl border border-[#dfe6dc] bg-gradient-to-br from-[#f4f8f2] via-white to-[#eef4ff] p-6 sm:p-8"><div className="flex flex-wrap gap-2 text-[10.5px] font-semibold"><span className="rounded-full bg-white px-3 py-1">{program.degreeLevel}</span><span className="inline-flex items-center gap-1 rounded-full bg-[#eef4ff] px-3 py-1 text-[#2563eb]"><ShieldCheck className="size-3"/>Official French source verified</span></div><h1 className="mt-5 text-[28px] font-semibold leading-tight tracking-[-0.03em] text-[#1b1b1b] sm:text-[36px]">{program.title}</h1>{institutionPath?<Link href={institutionPath} className="mt-3 inline-flex items-center gap-1.5 text-[14px] font-semibold text-[#4f4d48] hover:text-[#3e7a2e] hover:underline"><Building2 className="size-4"/>{program.institutionName}</Link>:<p className="mt-3 text-[14px] font-semibold text-[#4f4d48]">{program.institutionName}</p>}<div className="mt-3 flex flex-wrap gap-x-5 gap-y-2 text-[12px] text-[#77746e]">{program.city&&<span className="inline-flex items-center gap-1.5"><MapPin className="size-4"/>{program.city}</span>}{program.languageContext&&<span className="inline-flex items-center gap-1.5"><Languages className="size-4"/>{program.languageContext}</span>}</div></header><section className={`mt-5 rounded-xl border p-5 ${admission.className}`}><h2 className="text-[14.5px] font-semibold">{admission.label}</h2><p className="mt-2 text-[12px] leading-5">{admission.body}</p>{program.intakeLabel&&<p className="mt-3 text-[11px] font-semibold">Verified academic year: {program.intakeLabel}</p>}{program.applicationDeadline&&<p className="mt-1 text-[11px]">Source-backed deadline: {program.applicationDeadline}</p>}</section><div className="mt-5 grid gap-3 sm:grid-cols-2"><Metric icon={<GraduationCap className="size-4"/>} label="Study level" value={program.degreeLevel}/><Metric icon={<Clock3 className="size-4"/>} label="Duration" value={durationLabel(program.durationMonths)}/><Metric icon={<Languages className="size-4"/>} label="Language context" value={program.languageRequirementContext??program.languageContext??"Confirm with provider"}/><Metric icon={<CalendarClock className="size-4"/>} label="Admission status" value={program.internationalAdmissionStatus.replaceAll("_"," ")}/></div><section className="mt-5 rounded-xl border border-[#e7e6e3] bg-white p-5 sm:p-6"><h2 className="text-[14.5px] font-semibold">Source semantics</h2><p className="mt-3 text-[12px] leading-6 text-[#65625c]">{program.sourceVerificationLabel}</p><p className="mt-2 text-[11px] leading-5 text-[#8f8c85]">French national degree and quality-assurance frameworks are not converted into a separate CampCareer programme-level accreditation claim without matching an individual decision-level record.</p></section>{program.occupationIds.length>0&&<section className="mt-5 rounded-xl border border-[#e7e6e3] bg-white p-5 sm:p-6"><h2 className="text-[14.5px] font-semibold">CampCareer career links</h2><div className="mt-3 flex flex-wrap gap-2">{program.occupationIds.map(id=><span key={id} className="rounded-full bg-[#f3f4f1] px-2.5 py-1 text-[10.5px] font-medium text-[#686660]">{id.replace(/-/g," ")}</span>)}</div><p className="mt-3 text-[10.5px] leading-5 text-[#8f8c85]">These are reviewed education relationships, not claims of French professional recognition, protected-title eligibility, licensing, visa eligibility or guaranteed employment.</p></section>}</main><aside className="rounded-2xl border border-[#e7e6e3] bg-white p-5 lg:sticky lg:top-20"><h2 className="text-[14px] font-semibold">Source status</h2><dl className="mt-4 space-y-3 text-[11.5px]"><div className="flex justify-between gap-4"><dt className="text-[#8f8c85]">Programme source</dt><dd className="font-semibold">Tier {program.verificationTier}</dd></div><div className="flex justify-between gap-4"><dt className="text-[#8f8c85]">Admission evidence</dt><dd className="font-semibold">{program.admissionVerificationStatus}</dd></div><div className="flex justify-between gap-4"><dt className="text-[#8f8c85]">Programme accreditation claim</dt><dd className="font-semibold">{program.hasProgrammeAccreditationClaim?"Yes":"No"}</dd></div></dl><div className="mt-5 space-y-2">{programmeUrl&&<SourceLink href={programmeUrl} primary>University programme source</SourceLink>}{admissionUrl&&admissionUrl!==programmeUrl&&<SourceLink href={admissionUrl}>Admission source</SourceLink>}</div><p className="mt-4 text-[10.5px] leading-5 text-[#aaa7a0]">Mon Master, university and Études en France routes can differ by applicant. Unknown values remain unknown until a current source is verified.</p></aside></div></div>}
function Metric({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="rounded-xl border border-[#e7e6e3] bg-white p-4"><div className="flex items-center gap-2 text-[#8f8c85]">{icon}<span className="text-[10.5px] font-semibold uppercase tracking-[0.06em]">{label}</span></div><p className="mt-2 text-[13px] font-semibold text-[#34332f]">{value}</p></div>}
function SourceLink({href,primary=false,children}:{href:string;primary?:boolean;children:React.ReactNode}){return <a href={href} target="_blank" rel="noreferrer" className={primary?"flex items-center justify-center gap-2 rounded-lg bg-[#3e7a2e] px-4 py-2.5 text-[12px] font-semibold text-white":"flex items-center justify-center gap-2 rounded-lg border border-[#cfd9ca] px-4 py-2.5 text-[12px] font-semibold text-[#3e7a2e]"}>{children}<ArrowUpRight className="size-3.5"/></a>}
